<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h1>Editing Book</h1>
      <%= form_with model: @book do |f| %>
      <div class="form-group">
        <label for="inputtitle">本のタイトル</label>
        <%= f.text_field :title, autofocus: true, id:"title", placeholder:"本のタイトル"%>
      </div>
      <div class="form-group">
        <label for="inputbody">感想</label>
        <%= f.file_field :book,　autofocus: true, id:"body", placeholder: "感想" %>
      </div>
        <%= f.submit "Update Book" %>
      <% end %>
      <%= link_to "Show", book_path(@book.id) %>
      |
      <%= link_to "Back", books_path(@books) %>
    </div>
  </div>
</div>